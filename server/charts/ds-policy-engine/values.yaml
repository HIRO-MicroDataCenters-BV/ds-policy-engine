replicaCount: 1

image:
  repository: ""
  tag: ""
  pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: "ds-policy-engine"

serviceAccount:
  create: true
  annotations: {}
  name: ""

podAnnotations: {}
podLabels: {}
podSecurityContext: {}
securityContext: {}

container:
  command: ["uvicorn"]
  args: ["app.main:app", "--host", "0.0.0.0", "--port", "8000"]
  port: 8000

service:
  type: ClusterIP
  port: 8000

livenessProbe:
  httpGet:
    path: /health
    port: 8000
  initialDelaySeconds: 15
  periodSeconds: 20
readinessProbe:
  httpGet:
    path: /health
    port: 8000
  initialDelaySeconds: 5
  periodSeconds: 10

ingress:
  enabled: true
  className: ""
  annotations: {}
  host: ds-policy-engine.nextgen.hiro-develop.nl
  path: /
  pathType: Prefix

persistence:
  enabled: true
  accessModes: [ReadWriteOnce]
  size: 1Gi
  storageClass: ""
  mountPath: /app/data

cors:
  # Comma-separated list exposed to the app as DS__CORS_ALLOWED_ORIGINS.
  allowedOrigins: ""

docs:
  # When false the chart sets DS__DOCS_ENABLED=false, disabling /docs, /redoc, /openapi.json.
  enabled: true

env:
  logLevel: INFO
  nodeName: local

database:
  # sqlite+aiosqlite default persists at {{ .Values.persistence.mountPath }}/policy_engine.db
  url: sqlite+aiosqlite:////app/data/policy_engine.db

opa:
  enabled: true
  image:
    repository: openpolicyagent/opa
    tag: latest
    pullPolicy: IfNotPresent
  port: 8181
  # Mounted as /config/opa-config.yaml if provided; otherwise OPA starts with defaults.
  configYaml: ""
  resources:
    requests:
      memory: "64Mi"
      cpu: "50m"
    limits:
      memory: "128Mi"
      cpu: "200m"

resources:
  requests:
    memory: "128Mi"
    cpu: "100m"
  limits:
    memory: "512Mi"
    cpu: "500m"

namespace: ""
nodeSelector: {}
tolerations: []
affinity: {}

extraEnv: []
